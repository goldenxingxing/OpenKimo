# Runtime deps installed via pip into runtime/site-packages/ during build_windows.py
# (kimi-cli itself is installed from the locally-built wheel, not from PyPI)

pystray>=0.19
Pillow>=10.0
pywebview>=5.0
playwright>=1.40
